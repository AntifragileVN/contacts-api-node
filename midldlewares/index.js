const validateBody = require('./validateBody.js');
const isValidId = require('./isValidId.js');
const authentificate = require('./authentificate.js');
const upload = require('./upload.js');

module.exports = {
	validateBody,
	isValidId,
	authentificate,
	upload,
};
